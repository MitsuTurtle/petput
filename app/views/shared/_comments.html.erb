          <div>
            <% if comments %>
              <% comments.each do |comment| %>
                <div class="nickname-comment">
                  <%= link_to comment.user.nickname, "#", class:"nickname" %>
                  <span class="comment"><%= comment.text %></span>
                </div>
              <% end %>
            <% end %>
          </div>