

<div>
  <%# あとでアバターを入れる %>

  <%= notification_form(notification).html_safe %><span><%= " (#{time_ago_in_words(notification.created_at)} 前)" %></span>
  <% if !@comment.nil? %>
    <p><%= @comment %></p>
  <% end %>
</div>