<div class="registration-edit-page">
  <div class="registration-form-wrap">

    <%= link_to image_tag("petput-logo.png", class:"registration-petput-icon"), "/" %>

    <h2 class="registration-catchword">登録情報を変更</h2>

<%
=begin
%>

    <%= link_to 'Facenoteでログイン', "#", class: 'facebook-link' %>

    <p class="or">または</p>

<%
=end
%>

    <%= form_with model: @user, url: user_registration_path, local: true do |f| %>
      <%= render "devise/shared/error_messages", model: f.object %>

      <div class="field">
        <%= f.label :nickname, "ユーザーネーム", class: "label" %>
        <%= f.text_field :nickname, placeholder: "ユーザーネーム", autofocus: true, autocomplete: "off", class: "field-box" %>
      </div>

      <div class="field">
        <%= f.label :avatar, "プロフィール写真を選択", class: "label" %>
        <div class="profile-pic-btn field-box">
          プロフィール写真を選択
          <%= f.file_field :avatar, class: "profile-pic-input" %>
        </div>
      </div>


      <div class="field">
        <%# <%= f.label :email %>
        <%# <br /> %>
        <%= f.label :email, "メールアドレス", class: "label" %>
        <%= f.email_field :email, autofocus: true, placeholder: "メールアドレス", autocomplete: "email", class: "field-box" %>
      </div>

<%
=begin
%>

      <div class="field">
        <%# <%= f.label :password %>
        <% if @minimum_password_length %>
        <%# <em>(<%= @minimum_password_length %> <%# characters minimum)</em> %>
        <% end %><%# <br /> %>
        <%= f.password_field :password, placeholder: "パスワード（英数字混合6文字以上）", autocomplete: "new-password", class: "field-box" %>
      </div>

      <div class="field">
        <%# <%= f.label :password_confirmation %><%# <br /> %>
        <%= f.password_field :password_confirmation, placeholder: "パスワード（確認用）", autocomplete: "new-password", class: "field-box" %>
      </div>

<%
=end
%>

      <div class="field">
        <%# <%= f.label :current_password %> <%# <i>(we need your current password to confirm your changes)</i><br /> %>
        <%= f.label :current_password, class: "label" do %>
        現在のパスワード <span class="required-btn">必須</span>
        <% end %>
        <%= f.password_field :current_password, placeholder: "現在のパスワード", autocomplete: "current-password", class: "field-box" %>
      </div>

      <div class="profile-field">
        <%= f.label :profile, "自己紹介", class: "label" %>
        <%= f.text_area :profile, placeholder: "自己紹介", autocomplete: "off", class: "profile-field-box" %>
      </div>

      <div class="actions">
        <%= f.submit "更新する", class: "update-button" %>
      </div>
    <% end %>

<%
=begin
%>

    <p class="registration-consent">登録することで、<span class="font_bold">Facenoteの利用規約、データに関するポリシー、Cookieポリシー</span>に同意するものとします。</p>

  </div>

  <div class="login-link-wrap">
    <%# <%= render "devise/shared/links" %>
    <p class="registraion-question">アカウントをお持ちですか？</p>
    <%= link_to "ログインする", new_session_path(resource_name), class: "login-link" %>
  </div>
<%
=end
%>

</div>




<%# ↓デフォルトのコード %>
<%
=begin
%>

<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", model: f.object %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <%= f.label :password %> <i>(leave blank if you don't want to change it)</i><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> characters minimum</em>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password" %>
  </div>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

<h3>Cancel my account</h3>

<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

<%= link_to "Back", :back %>

<%
=end
%>